[build]
  #publish = "exampleSite/public"
  #command = "cd exampleSite && hugo --gc --themesDir ../.."
  #command = "cd exampleSite && hugo --gc --themesDir ../../"
  command = "hugo"
  
[build.environment]
  HUGO_VERSION = "0.60.1"
  HUGO_THEME = "repo"
